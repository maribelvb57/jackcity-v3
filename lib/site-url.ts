/**
 * URL pública del sitio. La usan el sitemap, el robots.txt y los datos
 * estructurados, que necesitan URLs absolutas.
 */
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000"
